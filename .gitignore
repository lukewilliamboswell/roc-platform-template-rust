# Build artifacts
target/
generated-docs/

# Roc source (built from pinned commit in CI)
roc-src/

# Platform binaries (but not musl runtime files)
*.so
*.dylib
*.dll
# Ignore .a files except musl runtime libraries
*.a
!platform/targets/*/libc.a
!platform/targets/*/libunwind.a

# Object files (but not musl crt1.o)
*.obj
*.lib
*.o
!platform/targets/*/crt1.o

# Roc build artifacts
*.rh
*.rm
*.wasm
*.ll

# Archives
*.tar.br
*.tar.gz

# OS files
.DS_Store

# Cargo lock (we're a library/binary, not an application)
Cargo.lock

# Claude Code
.claude
