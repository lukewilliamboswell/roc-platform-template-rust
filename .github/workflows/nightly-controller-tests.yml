name: Nightly configuration
on:
  pull_request:
  workflow_dispatch:
    inputs:
      nightly_validation:
        description: Validate a nightly candidate
        type: boolean
        default: false
permissions:
  contents: read
jobs:
  configuration:
    uses: lukewilliamboswell/roc-automation/.github/workflows/check-config.yml@355f762079f676df4d0ae86416edf34216b488e0
